{
  "name": "plug-app",
  "version": "1.0.0",
  "description": "Smart plug control app",
  "scripts": {
    "build": "echo 'no build step, static www folder is used as-is'"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2"
  }
}
